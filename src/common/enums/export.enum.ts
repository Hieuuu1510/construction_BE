export enum ExportType {
  PDF = "pdf",
  CSV = "csv",
  XLSX = "XLSX",
}

export enum UploadMimeTypeFile {
  PDF = "application/pdf",
  CSV = "text/csv",
  XLSX = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
}
